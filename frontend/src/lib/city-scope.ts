/**
 * Область запуска по городам.
 *
 * Пока платформа работает в одном городе, интерактивный *фильтр по городу*
 * (поле «Город» в hero и в каталогах заведений/мастеров) прятать незачем —
 * фильтровать нечего. Ставим LAUNCH_CITY = null при выходе в мультигород, и
 * весь фильтр возвращается без переписывания компонентов.
 *
 * Не затрагивает: SEO-хабы /venues/city/{slug} и sitemap, упаковку city/cities
 * в URL (cities-http.ts), выбор города при регистрации заведения (CityCombobox).
 */
export const LAUNCH_CITY: string | null = "Казань"

export const isSingleCity = LAUNCH_CITY !== null
